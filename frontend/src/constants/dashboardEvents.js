/** Dispatched after weekly classroom schedule is saved (dashboard should refresh). */
export const SCHEDULE_SAVED_EVENT = 'ush-schedule-saved';

/** Classroom renamed, archived, deleted, etc. — refresh dashboard stats & lists. */
export const CLASSROOM_LIST_CHANGED_EVENT = 'ush-classroom-list-changed';
